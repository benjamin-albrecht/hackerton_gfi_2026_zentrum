server:
  port: 8080

spring:
  application:
    name: pdf-extractor
  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB

zentrum:
  storage:
    path: data/extractions

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
